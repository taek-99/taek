11. DBMS에 저장된 데이터는 다른 사용자에게 공유 될수 있어야함 - 완전 프라이빗 아님

12. 테이블명과 컬럼명엔 숫자가 들어갈수 있지만 숫자로 시작할순 없음

13. ORDER BY절은 GROUPBY절에 정의된것만 사용 가능

14. DISTINCT는 중복 제거고 컬럼 순서랑 관계 없음

15. ROUND는 반올림, TRUNC는 내림 날짜에도 적용가능, 아무것도 안써져 있으면 최소 단위로 함. (시간부터)

16. CEIL, FLOOR, MOD, SIGN은 이제 알아야함

17. LTRIM은 지정 문자 삭제 - 왼쪽부터 출발해서 다른문자 나오면 멈춤/ SUBSTR은 몇번째 문자부터 몇번째 문자까지 출력/ LENGTH(REPLACE(어쩌구)) 지정 문자를 지우고 남은 문자 갯수를 셈

18. CASE WHEN THEN은 세트 ELSE는 마지막에만 나옴 이중 CASE시 ELSE 중간에 사용 가능

19. NVL 형식이 안맞아서 실행되지 않음

20. COUNT GRROUP BY시 그룹조차 생성되지 않으면 공집합

21. SUM에 NULL이 있으면 더해지지 않는다

22. IS NOT NELL은 NULL이 아닌 경우를 찾아낼때 사용 

23. WHERE절엔 그룹함수 사용이 불가능, AS 사용 시 띄어쓰기나 대소문자구분, 특수기호 사용시엔 "" 필요함

24. 문자열 비교시엔 맨앞자리 숫자로 비교

25. DESC를 사용하면 NULL이 가장 앞으로 옴

26. LEFT OUTER JOIN은 이제 알아야지

27. RIGHT OUTER와 JOIN OUTER는 결과가 같을 수도 있다

28. 하나의 로우에 해당하는 스칼라 서브쿼리 결과가 0건이면 생략되지 않는다.

29. 이름이 JAMES이고 이사람보다 늦게 태어난 사람들의 SAL값을 더한값

30. 각 그룹별 최고 월급을 받는 사람들의 합

31. 1번이 맞고 min구매일은 과거 max구매일은 최근을 뜻함

32. open인 애들은 제외하고 남은 애들을 더해라

33. any는 둘중 하나라도 참이면 그값을 반환함

34. UNION ALL연산자는 조회 결과를 정렬하고 중복되는 데이터를 여러번 표현할수 잇다

35. ROLLUP(DEPTNO)가 정답

36. PERCENT_RANK는 누적된 순위가 아닌 1 안에서 퍼센트를 출력

37. 자기 앞뒤를 더하는건데 이럴때 1PRECENDING AND 1 FOLLOWING을 사용, 행별로 값이 다르기 때문에 RANGE가 이닌 ROW 사용

38. SQL-SERVER의 TOP(N) 쿼리를 사용하면 정렬 순서대로 상위 N개 이상의 데이터 출력 가능

39. 사원별 관리자코드 레밸 연결후 경기인 사람만 출력

40. NULL값인것만 출력

41. UNPIVOT시 출력 결과와 반대로 작성

42. []안에 있는것은 다 지워버림.

