# js

## 객체구조
- object
- 중괄호를 이용해 구조
- 파이썬의 딕셔너리
- key는 문자형만 허용

## 속성 참조
- .표기법 또는 대괄호표기법으로 객체 속성에 접근
- in 연산자 사용시 hasOwnProperty 메서드 사용 권장
- 평소에는 .을 쓰고 동적으로 변수에 접근해야할때는 []사용
- 메서드
    - 객체 속성에 정의된 함수

## this
- 함수안에서 객체를 호출하는것
- 호출방법
    - 일반함수에서의 단순호출: 전역 객체
- 객체에서의 메서드 호출
    - 메서드를 호출한 객체

- forEach의 인자로 전달된 콜백 함수는 일반함수로 호출되므로, this는 전역 객체를 가리킴
- 함수는 호출될때 this를 암묵적으로 전달 받음
- 함수가 호출되는 방식에 따라 결정되는 현재 객체를 나타냄
- 장점
    - 함수를 하나만 만들어 여러 객체가 공유하여 각자 자신의 데이터로 동작하게 할 수 있음
- 단점
    - 이런 유연함이 실수로 이어질수 있다는것

## 추가 객체 문법
- 단축속성
    - 키이름과 값으로 쓰이는 변수의 이름이 같으면 단축해서 사용 가능

- 단축 메서드
    - 메서드 사용시 function 생략 가능

- 계산된 속성

- 옵셔널 채이닝
    - 속성이 없는 중첩 객체에 접근하려 할 때 에러 발생 없이 안전하게 접근하는 방법

## Array Helper Methods
- 배열 조작을 보다 쉽게 수행할 수 있는 특별한 메서드 모음
- ES6에 도입

- forEach
    - 배열 내의 모든 요소 각각에 대해 함수를 호출
- map
    - 배열 내의 모든 요소 각각에 대해 함수를 호출
    - 함수 호출 결과를 모아 새로운 배열을 반환

## 콜백 함수
- 다른 함수에 인자로 전달되는 함수
- 외부 함수 내에서 호출되어 일종의 루틴이나 특정 작업을 진행
- 특정 작업이 완료된 후, 시스템에 의해 나중에 호출되는 함수
